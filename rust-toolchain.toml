# Pin the toolchain so formatting, lints, and builds are reproducible across
# contributors and CI. Bump deliberately (it can change rustfmt/clippy output).
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
