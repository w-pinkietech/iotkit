[package]
name = "iotkit-core-mqtt-contract"
version = "0.1.0"
edition = "2024"

[dependencies]
iotkit-core-types = { path = "../types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
percent-encoding = "2"
thiserror = "2"

[dev-dependencies]
