# 外部codexレビュー第2回 裁定表(2026-07-03)

実行: codex exec / gpt-5.5 / reasoning effort xhigh / read-only。スコープ: 全コーパス(第1回反映済み6件を除外指示)。
指摘: [高]4 / [中]4 / [低]1 / 実装照合1。裁定者: メイン対話(Fable)。

| # | 指摘 | 裁定 | 対処 |
|---|---|---|---|
| 高1 | 送信者ID=認証主体かEnvelope.sourceか未固定 | **採用** | D1監査追記: dedup・認可は常に認証主体から導出。sourceは診断用自己記述。認証付きバインディングで不一致=rejected+侵害シグナル監査(実装Wave 1) |
| 高2 | dispositionが直交状態(staged×quarantined)を1値に潰す | **採用(直列性の明文化)** | D1監査追記: subject解決が常に先でstagedとquarantinedは同時に成立しない(レジストリ判定は承認時本流化で実施)。優先順位 staged>quarantined>durable。`quarantine_reason` 任意フィールドをadditive追加(実装はレジストリ実装と同時) |
| 高3 | Wave 0のreplace-hardware突合がキュー5(能力宣言形式)に依存 | **採用** | D5 Wave 0スコープ: 突合は**観測プロファイル**(staged/直近readingsから導出したmeasurement_key×channel集合)を宣言の代用に使う——依存を切断 |
| 高4 | アーカイブ責任消費者ack vs R17劣化契約の優先順位未定義 | **採用** | D2 §1: パージ順序=①ack済みフロア超過→②検疫滞留→③未ack正本。③はcustody_lost監査イベント+出口欠落annotation必須。無音の正本破棄は契約違反。台帳バッファ節にも参照追記 |
| 中5 | time_quality(R18)が取り込み契約に不在 | **部分採用** | time_qualityは受信側(ゲートウェイ)刻印でありエンベロープには載せない(送信者は主張できない)と明文化。readings v3に列追加(Wave 0はDEFAULT 'unsynced'固定、評価実装はWave 1)。実装計画1 Task 5に反映済み |
| 中6 | 未知キーの後付けマッピング後の既存series昇格が未定義 | **採用** | D6決定3: エイリアス確立時の遷移規則——実体化済みseriesは申告キーのまま定義バインド+検疫解除(履歴を切らない)、未実体化は以後canonicalで実体化。昇格・付け替えはしない |
| 中7 | legacy_sensor_type(整数)がキー文法と衝突 | **採用** | D6決定3: legacy_sensor_typeはワイヤエイリアスではなく移行シム専用の型付き対応表(整数→キー)。整数文字列は文法違反でワイヤに乗らないため構造的に混入しない |
| 中8 | R22スナップショットとoperatorトークンの連動が曖昧 | **採用** | D2 R22最小契約: secretsにoperatorトークンハッシュを明記。復元でトークンは有効なまま(復旧直後にAI/人間が入れる保証)。失効はR14明示操作。エポックフェンスが拒むのは旧エポックの箱であって新しい箱上のトークンではない |
| 低9 | 用語集「取り込み契約」行にMQTT欠落 | **採用** | バインディング列挙にMQTT ingest専用リスナーを追加 |
| 実装照合 | ReadingItem.values: Vec\<f64\> がD6値型体系(bool/int/record)を表現できない | **部分採用** | D6決定10にワイヤ表現v1を明文化: スカラー系は全てf64配列(bool=0/1、int=f64化で53bit精度十分)。値型はカタログ側の検証・解釈情報。recordワイヤ表現は第二波追補でadditiveに定義(=契約予約の実体)。契約型の変更はしない |

## 棄却なし

今回は10件全てが採用または部分採用。部分採用2件の不採用部分:
- 中5のうち「エンベロープにtime_qualityフィールドを追加」→ 不採用(送信者が主張できない受信側メタデータのため)
- 実装照合のうち「契約型をValue enumへ変更」→ 不採用(f64正規化+additive追補で足り、契約破壊を避ける)
